# 7me – Private Beta Deployment

## Installation
1. Extract or clone the repository.
2. Run `npm install` in the root folder.
3. Run `npm run start:all` to start backend, AI service, and mobile app simultaneously.

Works on Windows, Mac, and Linux.
